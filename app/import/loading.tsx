import { PageSkeleton } from "@/components/page-skeleton";

export default function ImportLoading() {
  return (
    <PageSkeleton
      title="นำเข้าข้อมูล"
      description="กำลังเตรียมหน้าจอนำเข้าข้อมูล"
      variant="detail"
    />
  );
}
